<br><br>
<%= form_for(@splace_category) do |f| %>
  <% if @splace_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@splace_category.errors.count, "error") %> prohibited this splace_category from being saved:</h2>

      <ul>
      <% @splace_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :code %>&nbsp;&nbsp;
    <%= f.text_field :code %>
  </div>
  <div class="field">
    <%= f.label :name %>&nbsp;&nbsp;
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
